<div class="modal fade" id="form">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Confirmar turno</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">


<%= form_for @event do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this paciente from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



    <div class="form-group">
    <%= f.label :start_date, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.datetime_field :start_date, class: "form-control", disabled: true %>
      <%= f.datetime_field :end_date, class: "form-control", disabled: true %>
    </div>
  </div>
    <div class="form-group">
    <%= f.label :text, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :text, size: "30x10", value: "Observaciones" ,class: "form-control" %>
    </div>
  </div>

<%= f.hidden_field :medico, value: @medico.id %>
<%= f.hidden_field :paciente, value: @paciente.id %>
<%= f.hidden_field :equipo, value: @equipo.id %>
<%= f.hidden_field :start_date, value: @fecha_inicio %>
<%= f.hidden_field :end_date, value: @fecha_fin %>
<%= f.submit 'Confirmar', :class => "btn btn-primary pull-right" %>


<% end %>
      </div>
    </div>
  </div>
</div>

